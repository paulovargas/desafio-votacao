spring:
  config:
    activate:
      on-profile: local
    import: optional:file:.env[.properties]
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/desafio_votacao}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
